# Система аксиом МТС

Этот документ фиксирует компактный нормативный набор обязательств принятого основания МТС. Мотивация и разъяснения находятся в [Основаниях МТС](Основания%20МТС.md).

<!-- mts-current-projection:start -->
> **Автоматическая проекция текущей машинной границы.** Этот блок строится из принятого контракта командой `npm --prefix ts run docs:sync`; содержательную теорию вне блока команда не меняет.
>
> - Текущий выпуск: `mts-contract/v0.9` + `mts-conformance/v0.9`; предыдущая неизменяемая пара: `mts-contract/v0.8` + `mts-conformance/v0.8`.
> - Семантическая база: `mts-contract/v0.8`; наблюдаемое смысловое изменение: `да`.
> - Реализация: `TypeScript`; единственная действующая смысловая среда: `да`; Python присутствует: `нет`; режим совместимости выбираем: `нет`.
> - Внутренние знаки F1: `∞ [ ] 1 0 ( ) ⟼ : =`; отложены: `≡ ♂ ♀ ↑ { } ¬ ↛`; только метаязык: `∧ ⇔ ⇒ ≠ →`.
> - Корневой базис: `R: R = R ⟼ R; O: O = O ⟼ R; C: C = R ⟼ C; L: L = O ⟼ C; U: U = C ⟼ O`.
> - Чтение может материализовать: `нет`; отсутствие найденной записи доказывает несуществование: `нет`.
> - Строковый носитель: единица `byte`, конверт `[bbbbbbbb]`, бит на конверт `8`, порядок `MSB-first`.
<!-- mts-current-projection:end -->

Исторические версии сохраняются в Git и не определяют текущую семантику автоматически.

## А0. Онтологическая замкнутость

```text
всё есть связь
```

Любое семантически существенное различие должно быть выражаемо связью или сетью связей.

```text
1 → L = O ⟼ C
0 → U = C ⟼ O
0 ≠ DELETE_LINK
```

Несвязь не вводится как вторая первичная сущность.

## А1. Самозамыкание — первичный источник различения

В теоретической метанотации:

```text
∞
♂e
b♀
b ⟼ e
```

Структурно:

```text
R = R ⟼ R
S = S ⟼ e
E = b ⟼ E
X = b ⟼ e
```

Несамозамкнутый полюс должен быть уже различён самозамыканием или связанностью с ранее различёнными связями. `♂` и `♀` здесь — остенсивная метанотация; в F1 v0.9 они отложены как внутренние знаки.

## А2. Акорень единственен

```text
R = ∞
R = R ⟼ R
```

Если `X = X ⟼ X`, то:

```text
X = R = ∞
```

Технический идентификатор не создаёт второй полностью самозамкнутый смысловой корень.

## А3. Однополюсное самозамыкание различает ориентацию

```text
♂e = S = S ⟼ e
b♀ = E = b ⟼ E
```

Для фиксированного уже различённого внешнего полюса соответствующая форма единственна.

## А4. Бинарность и направленность

```text
X = b ⟼ e
```

Полная связь имеет два упорядоченных различённых полюса. В общем случае:

```text
b ⟼ e ≠ e ⟼ b
```

## А5. Пятисвязный корневой базис

```text
R = ∞
R = R ⟼ R
O = O ⟼ R
C = R ⟼ C
L = O ⟼ C
U = C ⟼ O
```

Корневой словарь:

```text
∞ → R
[ → O
] → C
1 → L
0 → U
```

`[ ] 1 0` — ровно четыре абита; `∞` не является пятым.

## А6. Тождество связи определяется полюсами

```text
(A ⟼ B) = (C ⟼ D)
⇔
A = C ∧ B = D
```

Технические ссылки, адреса и номера операций не входят в смысловое тождество.

## А7. Различимость распространяется от корня

```text
самозамыкание
→ R
→ O / C
→ L / U
→ связи с уже различёнными полюсами
→ дальнейшая асеть
```

## А8. Петля отличается от полного самозамыкания

```text
Loop(a) = a ⟼ a
```

обычно не равно `a`. Но если:

```text
a = a ⟼ a
```

то `a = R = ∞`.

Замкнутый остенсивный шаблон `♂♀` также схлопывается в `∞`, потому что обе предполагаемые связи получают одну и ту же пару полюсов.

### Конечная левая итерация

```text
a¹ = a
aⁿ⁺¹ = aⁿ ⟼ a,  n >= 1
```

`n` принадлежит метаязыку и не является внутренним числом МТС.

## А9. Роли выражаются связями

Тип, теория, правило, контекст или роль не являются скрытыми полями примитивной связи.

```text
T ⟼ Rule
```

выражает допуск правила связевой структурой.

## А10. Форма отделена от действия

```text
форма ≠ команда
```

Поиск, проверка, разрешение и материализация — разные акты. Наличие правила или функции как данных не означает выполнения.

## А11. Контекст явен

```text
P = parent ⟼ current
K = K ⟼ P
I ⟼ K
```

`current(K)` — метанотация. Внутренний знак `↑` в F1 v0.9 не принят. Лексическая вложенность хранится через `K.parent`, временной порядок — через `Run`/`Act`.

## А12. Источник отделён от формы

```text
точные байты
→ STRING→QUATERNARY
→ связевое представление источника
→ история употребления
→ D / G / T
→ форма F
```

Один байт кодируется одним `[8 bits]`-конвертом. Представление, интерпретация и денотация различаются.

## А13. Словарь и история явны

```text
D = D ⟼ (parentScope ⟼ localHistory)
Entry = sourceContent ⟼ form
Definition = D_before ⟼ Entry
```

Различие событий определения хранится в истории, а не в технических экземплярах одной пары.

## А14. Чтение отделено от изменения

```text
читать / искать / разрешать / проверять / воспроизводить
                    ≠
создавать / удалять / изменять
```

```text
не найдено ≠ не существует
find ≠ materialize
```

## А15. Локальное отношение представителей не заменяет А6

```text
Pair    = member ⟼ representative
Binding = K ⟼ Pair
```

Оно не вводит автоматически глобальную подстановку, транзитивность или конгруэнтность.

## А16. Правило требует явного допуска

```text
Rule = DR ⟼ Body
T ⟼ Rule
```

Проверка структурного правила выполняется только чтением.

## А17. Акт, интерпретатор и результат различаются

```text
I — интерпретатор
A — акт
R — результат
```

Различие актов выражается структурой истории, а не номером экземпляра.

## А18. Циклы требуют укоренённой различимости

Самозамыкания, петли, совместное использование и взаимные циклы допустимы, если элементы структурно различены относительно корневого основания.

## А19. Физический адрес не является тождеством связи

Оперативный адрес, долговременный адрес, номер записи снимка и смысловая связь — разные уровни. Дубликаты одной структурно неразличимой формы канонизируются либо отклоняются.

## А20. Остенсивность предшествует машине

```text
∞ / ♂e / b♀ / b⟼e
→ структурное различение
→ тождество по полюсам
→ Link(start,end)
```

Машинный носитель не является источником теории.

# Производные результаты

## F2/F3. Нормализация и отсутствие смысловых экземпляров

Используются конечные формы:

```text
ROOT
START(F)
END(F)
PAIR(F,G)
```

с наименьшим отношением `≈`. Для каждого класса существует одна нейтральная к именам нормальная форма.

```text
нет рекурсивно выраженного различия
=> нет двух смысловых связей
```

Отсюда следуют единственность корня и однополюсных форм, тождество полной связи по полюсам и нейтральность псевдонимов.

## F4. Рекурсивная реверсия

```text
J(R)=R
J(O)=C
J(C)=O
J(L)=L
J(U)=U
```

Она отличается от простой перестановки полюсов `P`, обратного отношения навигации, отрицания и несвязи.

## F5. Счёт по степени связности

```text
0 -> U
1 -> L
D1 = L
D2 = L ⟼ L
D3 = (L ⟼ L) ⟼ L
...
```

Конечные положительные степени попарно различны и не образуют положительных циклов.

```text
U ⟼ L != L
```

Поэтому:

```text
S0(U)=L
S0(D)=D⟼L, D∈Pos
```

Получается производная алгебра Пеано поверх алгебры степеней связности.

`Count` считает явно различённые вхождения, а не смысловые копии связей.

## F6. Производные теории

Коллекции, конечные функции как данные, относительная навигация и прикладные интерпретации строятся поверх обычных связей, явной теории/контекста и структуры вхождений/происхождения.

Ни множество, ни функция, ни путь, ни прикладная роль не становятся новым корневым видом сущности.

Для чтений открытого мира сохраняется:

```text
не найдено
!= пусто
!= не существует
```

если нет явного свидетельства замкнутости.

## Историческая граница

Старые `♀F / F♂` относятся к исторической проекционной семантике. Остенсивные `♂e / b♀` остаются теоретической метанотацией, но `♂`/`♀` отложены как внутренние знаки F1 v0.9.

## Сжатая система

```text
А0   всё есть связь
А1   самозамыкание — первичный источник различения
А2   единственная полностью самозамкнутая связь — ∞
А3   однополюсное самозамыкание различает ориентацию
А4   бинарность и направленность
А5   R/O/C/L/U и четыре абита
А6   тождество по различённым полюсам
А7   различимость распространяется от корня
А8   петля отличается от полного самозамыкания
А9   роли выражаются связями
А10  форма отделена от действия
А11  контекст явен
А12  источник отделён от формы
А13  словарь и история явны
А14  чтение отделено от изменения
А15  локальные представители не переопределяют А6
А16  правила требуют явного допуска
А17  акт, интерпретатор и результат различаются структурно
А18  циклы требуют укоренённой различимости
А19  физический адрес не является смысловым тождеством
А20  остенсивность предшествует машине
F2/F3 конечная нормализация и отсутствие смысловых экземпляров
F4   рекурсивная реверсия отдельно от перестановки полюсов и навигации
F5   степени связности и производная арифметика Пеано
F6   коллекции, функции, навигация и приложения как производные теории
```

Машинный контракт и корпус соответствия фиксируют переносимую наблюдаемую границу; реализация `@mts/core` является её воплощением, а не источником теории.