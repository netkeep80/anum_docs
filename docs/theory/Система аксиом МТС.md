# Система аксиом МТС

Этот документ фиксирует компактный нормативный набор обязательств принятого основания MTS v0.7. Мотивация и разъяснения находятся в [Основаниях МТС](Основания%20МТС.md).

MTS v0.7 является текущей принятой версией. Исторические версии сохраняются Git и не определяют текущую семантику автоматически; будущие расширения требуют отдельного решения.

## А0. Онтологическая замкнутость

```text
всё есть связь
```

Любое семантически существенное различие должно быть выражаемо связью или сетью связей.

**Несвязь не вводится как отдельный вид существующего**. Её смысл может быть представлен связью, но это не превращает отсутствие связности во вторую первичную сущность.

В корневом базисе:

```text
1 → L = O ⟼ C
0 → U = C ⟼ O
0 ≠ DELETE_LINK
```

## А1. Самозамыкание является первичным источником различения

МТС не начинает с заранее различённых ID связей. Первичное различение возникает из остенсивного рисунка самозамыкания полюсов.

Четыре формы:

```text
∞
♂e
b♀
b ⟼ e
```

Структурно:

```text
R = R ⟼ R
S = S ⟼ e
E = b ⟼ E
X = b ⟼ e
```

Несамозамкнутый полюс должен быть уже различён самозамыканием или связанностью с ранее различёнными связями.

## А2. Акорень единственен

Существует ровно одна полностью самозамкнутая связь — акорень:

```text
∞
R = ∞
R = R ⟼ R
```

Если:

```text
X = X ⟼ X
```

то:

```text
X = R = ∞
```

Номер записи, адрес памяти, слот или временная ссылка построения не могут различить несколько полностью самозамкнутых связей.

## А3. Однополюсное самозамыкание различает ориентацию

Для уже различённого `e`:

```text
♂e = S = S ⟼ e
```

Для уже различённого `b`:

```text
b♀ = E = b ⟼ E
```

`♂` и `♀` не являются командами получения начала или конца. Они остенсивно показывают, какой полюс замкнут на саму связь.

Для одного и того же различённого внешнего полюса соответствующая однополюсная форма единственна.

## А4. Бинарность и направленность

Полная связь имеет два упорядоченных уже различённых полюса:

```text
X = b ⟼ e
```

В общем случае:

```text
b ⟼ e ≠ e ⟼ b
```

если порядок различённых полюсов действительно изменился.

## А5. Пятисвязный корневой базис

```text
R = ∞
R = R ⟼ R
O = O ⟼ R
C = R ⟼ C
L = O ⟼ C
U = C ⟼ O
```

Корневой словарь:

```text
∞ → R
[ → O
] → C
1 → L
0 → U
```

Остенсивно:

```text
O = O ⟼ R ≡ ♂∞
C = R ⟼ C ≡ ∞♀
```

`[ ] 1 0` — ровно четыре корневых абита. `∞` является основанием их различения, а не пятым абитом.

## А6. Тождество связи определяется полюсами

```text
(A ⟼ B) = (C ⟼ D)
⇔
A = C ∧ B = D
```

Следовательно:

```text
P1 = A ⟼ B
P2 = A ⟼ B
=> P1 = P2
```

В МТС не существует двух различных смысловых связей с одной и той же упорядоченной парой уже различённых полюсов.

Важно: А6 не означает сравнение сырых технических указателей. Сами `A`, `B`, `C`, `D` должны быть различены по А1–А5 либо через последующую связанность с уже различённой структурой.

## А7. Различимость распространяется от корня

Нормативная цепочка:

```text
самозамыкание
→ R
→ O / C
→ L / U
→ связи с уже различёнными полюсами
→ дальнейшая асеть
```

Технический ID не входит в эту цепочку.

Если две физические записи нельзя структурно различить относительно уже различённого основания, они не образуют две смысловые связи МТС.

## А8. Петля отличается от полного самозамыкания

Для данного `a`:

```text
Loop(a) = a ⟼ a
```

задаёт единственную связь этой пары.

Обычно:

```text
Loop(a) ≠ a
```

Но если:

```text
a = a ⟼ a
```

то это уже полное самозамыкание и по А2:

```text
a = R = ∞
```

В частности:

```text
Loop(R) = R
```

### Следствие А2 + А6. Взаимное самозамыкание `♂♀` схлопывается в акорень

Запись `♂♀` здесь не означает последовательное применение двух операторов и не вводит самостоятельные связи `♂` и `♀`. Это краткое обозначение **замкнутого остенсивного шаблона без внешнего уже различённого полюса**.

Временно обозначим связь, несущую самозамыкание начала, через `y`, а связь, несущую самозамыкание конца, через `x`:

```text
y = ♂x
x = y♀
```

На уровне полюсов это одна замкнутая система:

```text
y = y ⟼ x
x = y ⟼ x
```

У `x` и `y` совпадает одна и та же упорядоченная пара полюсов `(y, x)`. По А6:

```text
x = y
```

Обозначим эту единственную связь через `z`. Подстановка даёт:

```text
z = z ⟼ z
```

По А2 полностью самозамкнутая связь единственна, следовательно:

```text
z = R = ∞
```

и тем самым:

```text
♂♀ = ∞
```

Это не противоречит А3. Формы `♂e` и `b♀` являются однополюсными только пока второй полюс действительно внешне различён относительно самой связи. В замкнутом шаблоне `♂♀` попытка сделать два полюса взаимным основанием различения устраняет это различие и переводит систему в более специальный случай полного самозамыкания.

Следствие также запрещает трактовать два технических ID такой системы как две смысловые связи: ID не может предотвратить схлопывание по А6 и не создаёт второй акорень.

### Производная конструкция: конечная левая итерация связи

Для уже различённой связи `a` вводится **только как внешняя метанотация** положительная конечная степень:

```text
a¹ = a
aⁿ⁺¹ = aⁿ ⟼ a,  n >= 1
```

То есть:

```text
a² = a ⟼ a = Loop(a)
a³ = (a ⟼ a) ⟼ a
a⁴ = ((a ⟼ a) ⟼ a) ⟼ a
```

Рекурсия фиксирует левоассоциативность: на каждом шаге предыдущий результат является началом, а исходный `a` — концом новой связи.

`n` здесь принадлежит метаязыку описания конечного числа шагов. Это **не** внутреннее определение натурального числа МТС и не аргумент в пользу готовой арифметики внутри асети. В частности, `a⁰` и `a^∞` этой конструкцией не определяются.

По А6 каждый следующий шаг однозначен. Если два вычисления начинают с одной и той же `a` и выполняют одинаковое конечное число шагов, они получают одну и ту же смысловую связь; отдельное тождество вычислительного употребления не возникает.

Для акорня:

```text
R¹ = R
Rⁿ⁺¹ = Rⁿ ⟼ R
```

и по индукции на внешнем конечном индексе:

```text
Rⁿ = R,  n >= 1
```

поскольку базовый случай равен `R`, а шаг сводится к `R ⟼ R = R`.

Отдельной принятой операции инверсии в MTS v0.7 эта производная конструкция не вводит. Если в будущей теории инверсия будет определена как перестановка полюсов `Inv(b ⟼ e)=e ⟼ b`, то второй шаг является её неподвижной точкой:

```text
Inv(a²) = Inv(a ⟼ a) = a ⟼ a = a²
```

Это условное следствие симметрии петли, а не скрытое добавление `Inv` в текущий нормативный runtime.

### Производная структура: канонический натуральный ряд

Левую итерацию можно продолжить базой до первого положительного шага без введения нового атома. Выбираются:

```text
Z = R
G = C = R ⟼ C
S(n) = n ⟼ C
```

Здесь `Z` — роль базы **в этой производной структуре**. Это не означает глобального отождествления акорня с арифметическим знаком `0` и не имеет отношения к корневому абиту:

```text
0 → U
```

Первый шаг:

```text
S(R) = R ⟼ C = C
```

поэтому ряд начинается:

```text
R
C
C ⟼ C
(C ⟼ C) ⟼ C
((C ⟼ C) ⟼ C) ⟼ C
...
```

#### Почему генератором канонически является именно `C`

Пусть мы хотим одновременно сохранить три условия:

```text
Z = R
S_a(n) = n ⟼ a
N1 = a
```

Тогда из `N1=S_a(R)` следует:

```text
a = R ⟼ a
```

То есть `a` должна быть единственной end-self-closed формой над уже различённым началом `R`. По А3 и А5 это именно:

```text
a = C
```

Поэтому `C` не является произвольным выбранным ID или внешней меткой: это единственный корневой генератор, который продолжает принятую левую степень `a¹=a` назад базой `R`.

#### Натуральный домен

`Nat_C` определяется метатеоретически как **наименьший класс смысловых связей**, удовлетворяющий двум правилам:

```text
R ∈ Nat_C
n ∈ Nat_C  =>  S(n)=n ⟼ C ∈ Nat_C
```

Это определение не использует готовое внутреннее натуральное `n`. Конкретный член `Nat_C` является самой смысловой связью, а не позицией, номером записи или историей вычисления. Внешний конечный индекс используется только для разговора о членах ряда и доказательствах.

Для положительной части существует соответствие с предыдущей метанотацией:

```text
N0 = R
N1 = C = C¹
N2 = C ⟼ C = C²
N3 = (C ⟼ C) ⟼ C = C³
...
```

При этом `C⁰` по-прежнему не вводится: база `N0=R` задаётся непосредственно, а не через уже готовое возведение в нулевую степень.

#### Аналоги обязательств Пеано

**1. База.**

```text
R ∈ Nat_C
```

по определению.

**2. Замкнутость.**

Если `n ∈ Nat_C`, то:

```text
S(n)=n ⟼ C ∈ Nat_C
```

по правилу порождения.

**3. База не является следующим элементом.**

Предположим для некоторой связи `x`:

```text
S(x)=R
```

Тогда:

```text
x ⟼ C = R = R ⟼ R
```

По А6 пришлось бы получить одновременно:

```text
x = R
C = R
```

но `C ≠ R` в корневом базисе. Следовательно:

```text
S(x) ≠ R
```

для любой `x`.

**4. Инъективность следующего шага.**

Если:

```text
S(x)=S(y)
```

то:

```text
x ⟼ C = y ⟼ C
```

и по А6:

```text
x = y
```

**5. Индукция как минимальность.**

Пусть свойство `P` выполнено для `R` и сохраняется переходом `S`:

```text
P(R)
P(n) => P(S(n))
```

Поскольку `Nat_C` является наименьшим классом, содержащим `R` и замкнутым по `S`, свойство выполняется для каждого члена `Nat_C`. Это структурная индукция по конечному выводу членства, а не предположение уже готового внутреннего натурального индекса.

Из тех же пунктов следуют дополнительные свойства:

- каждый ненулевой в смысле этой структуры член `Nat_C` имеет предшественника;
- этот предшественник единственен по инъективности `S`;
- никакие два разных конечных шага от `R` не дают одну связь: иначе можно сокращать одинаковые внешние слои `S` до равенства `R` с положительным следующим шагом, что противоречит пункту 3.

Таким образом `Nat_C` даёт Peano-подобную структуру для сигнатуры «база + следующий элемент» непосредственно внутри универсума смысловых связей. Это ещё не определение сложения, умножения или полной арифметики.

#### Вырожденный генератор `R`

Если вместо `C` взять акорень как генератор:

```text
S_R(n)=n ⟼ R
```

то уже на базе:

```text
S_R(R)=R ⟼ R=R
```

и требование «база не является следующим элементом» нарушается. Поэтому `R` является важным отрицательным случаем и не порождает натуральный ряд по значениям связей.

#### Материализация не обязана быть бесконечной

`Nat_C` задаёт потенциально неограниченную производную структуру. Конкретная апамять не обязана заранее содержать весь ряд: каждый требуемый конечный элемент строится конечным числом канонических шагов, а отсутствие ещё не материализованного элемента не означает его структурной невозможности.

## А9. Роли выражаются связями

Примитивная связь имеет только начало и конец. Тип, теория, правило, контекст или роль не являются скрытыми обязательными полями примитива.

Например:

```text
T ⟼ Rule
```

выражает допуск правила самой связевой структурой.

## А10. Форма отделена от действия

```text
форма ≠ команда
```

Поиск, проверка, разрешение и материализация являются различными актами. Разные акты получения одной формы не создают несколько экземпляров результирующей связи.

## А11. Контекст явен

Кандидат контекста:

```text
P = parent ⟼ current
K = K ⟼ P
↑ = current(K)
```

Повтор той же полной формы `K` обозначает ту же связь. Если требуется различить два момента использования одного контекста, различие должно находиться в структуре истории актов.

## А12. Исходная запись отделена от формы

```text
байты
→ связевое представление источника
→ позиция / история употребления
→ словарь D
→ грамматика G
→ теория T
→ форма F
```

Написание знака не является самостоятельным семантическим авторитетом.

## А13. Словарь и история явны

Состояние словаря может быть выражено:

```text
D = D ⟼ (parentScope ⟼ localHistory)
```

Определение:

```text
Entry      = sourceContent ⟼ form
Definition = D_before ⟼ Entry
H_after    = H_before ⟼ Definition
D_after    = D_after ⟼ (parentScope ⟼ H_after)
```

Повтор уже существующей пары не создаёт копию связи. Если два события определения должны различаться, их различает структура предшествующей истории.

## А14. Чтение отделено от изменения

```text
читать / искать / разрешать / проверять
                    ≠
создавать / удалять / изменять
```

Критически:

```text
не найдено ≠ не существует
find ≠ materialize
```

Семантическая однозначность связи не означает, что её физическая запись уже присутствует в конкретной апамяти.

## А15. Локальное отношение представителей не заменяет А6

Дополнительное контекстное отношение может быть выражено:

```text
Pair    = member ⟼ representative
Binding = K ⟼ Pair
```

Оно не переопределяет фундаментальное тождество связи и не вводит автоматически глобальную подстановку, транзитивное замыкание или конгруэнтность.

## А16. Правило вывода требует явного допуска

```text
T ⟼ Rule
```

Правило не становится частью МТС только потому, что удобно реализации.

## А17. Акт, вычислитель и результат различаются структурно

Следует различать:

```text
I — вычислитель
A — акт
R — результат
```

Разные акты могут иметь один результат, но различие актов должно быть выражено их собственной связевой структурой или историей, а не скрытым номером экземпляра.

Последовательность актов может храниться как:

```text
Run_0 = R
Run_(i+1) = Run_i ⟼ A_i
```

Повтор одного и того же `A_i` в разных позициях не создаёт новую связь `A_i`.

## А18. Циклы допустимы только при структурной различимости

Асеть может содержать:

- однополюсное самозамыкание;
- петли `a ⟼ a`;
- совместное использование одной связи;
- повтор ссылок в последовательности;
- взаимные циклы, **если их элементы уже структурно различимы относительно корневого основания**.

Нельзя создать смысловое различие взаимного цикла только предварительной выдачей двум записям разных технических ID.

## А19. Физический адрес не является тождеством связи

Различаются:

```text
оперативный адрес
долговременный адрес
номер записи в снимке
смысловая связь
```

Перемещение записи не меняет связь. Несколько физических записей одной структурно неразличимой формы должны быть канонизированы либо отклонены на границе импорта.

Произвольный физический граф поэтому не тождественен готовой асети МТС:

```text
physical graph
→ structural canonicalization
→ semantic MTS network
```

## А20. Остенсивность предшествует машинному представлению

Правильный порядок:

```text
∞ / ♂e / b♀ / b⟼e
→ структурное различение
→ тождество по полюсам
→ Link(start,end)
```

`Link(start,end)` является техническим носителем уже понятой структуры, а не источником первичного тождества.

## Историческая граница

В принятой версии 0.2 использовались обозначения:

```text
♀F / F♂
```

Они относятся к исторической проекционной семантике и не определяют текущее самозамыкание.

Текущая ориентация:

```text
♂e = S = S ⟼ e
b♀ = E = b ⟼ E
```

## Сжатая система

```text
А0   всё есть связь; несвязь не является второй сущностью
А1   самозамыкание — первичный источник различения
А2   единственная полностью самозамкнутая связь — ∞
А3   ♂e / b♀ различают ориентацию самозамыкания
А4   бинарность и направленность
А5   R/O/C/L/U и четыре абита [ ] 1 0
А6   связь тождественна по уже различённым полюсам
А7   различимость распространяется от корня
А8   Loop(a)=a⟼a отличается от a=a⟼a
     следствие А2+А6: ♂♀=∞
     вывод: a¹=a; aⁿ⁺¹=aⁿ⟼a для внешнего n>=1
     вывод: Nat_C имеет Z=R и S(n)=n⟼C; C — канонический генератор
А9   роли выражаются связями
А10  форма отделена от действия
А11  явный K и ↑
А12  источник отделён от формы
А13  явные D и история
А14  не найдено ≠ не существует; find ≠ materialize
А15  локальные представители не переопределяют А6
А16  правила требуют явного допуска
А17  акт, вычислитель, результат и история различаются структурно
А18  циклы требуют укоренённой структурной различимости
А19  физический адрес не является semantic identity
А20  остенсивность предшествует машине
```

Эти аксиомы задают текущую принятую границу MTS v0.7. Всё, что выходит за неё, требует отдельного решения, а не скрытого расширения через реализацию.
