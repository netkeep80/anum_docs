# Система аксиом МТС

Этот документ фиксирует компактный нормативный набор обязательств текущего кандидата следующего основания МТС. Объяснения и мотивация находятся в [Основаниях МТС](Основания%20МТС.md).

Следующая версия ещё не принята. Принятые версии 0.2–0.5 остаются воспроизводимой историей.

## А0. Онтологическая замкнутость

```text
всё есть связь
```

Любое семантически существенное различие должно быть представимо связью или сетью связей.

## А1. Акорень

Существует различённый акорень:

```text
∞
∞ = ∞ ⟼ ∞
```

Самозамкнутая форма другой связи не делает её акорнем автоматически.

## А2. Четыре остенсивные формы

```text
∞
♂e
b♀
b ⟼ e
```

Структурно:

```text
R = R ⟼ R
S = S ⟼ e
E = b ⟼ E
X = b ⟼ e
```

`♂` и `♀` показывают самозамыкание соответствующего полюса и не являются встроенными командами вычислителя.

## А3. Самозамыкание не является типом

Форма:

```text
S = S ⟼ e
```

не означает сама по себе «незавершённость», а:

```text
Q = Q ⟼ Q
```

не означает автоматически `Q = ∞`.

Роль связи определяется её точным вхождением и явными свидетельствами конкретного акта.

## А4. Бинарность и направленность

Полная связь имеет два упорядоченных полюса:

```text
X = b ⟼ e
```

Обращение порядка создаёт другую ориентацию:

```text
b ⟼ e  ↔  e ⟼ b
```

## А5. Пятисвязный корень

```text
R = ∞
R = R ⟼ R
O = O ⟼ R
C = R ⟼ C
L = O ⟼ C
U = C ⟼ O
```

Корневой словарь:

```text
∞ → R
[ → O
] → C
1 → L
0 → U
```

Остенсивно:

```text
O = O ⟼ R ≡ ♂∞
C = R ⟼ C ≡ ∞♀
```

`[ ] 1 0` — корневые абиты; `♂ ♀ ⟼` — знаки формальной нотации форм связи.

## А6. Точное вхождение

Одинаковые полюса не задают тождество:

```text
P1 = A ⟼ B
P2 = A ⟼ B
P1 ≠ P2
```

Одинаковая форма, изоморфная топология или одинаковый номер в снимке также не заменяют тождество конкретного вхождения.

## А7. Роли выражаются связями

Примитивная связь имеет только начало и конец. Семантические роли не должны скрываться в обязательных полях типа, стадии или признака аксиомы.

Например допуск правила выражается связью:

```text
T ⟼ Rule
```

## А8. Форма отделена от действия

Остенсивная форма декларативна:

```text
форма ≠ команда
```

Поиск, проверка, разрешение и материализация определяются явным актом и предъявленными свидетельствами.

## А9. Контекст явен

```text
P = parent ⟼ current
K = K ⟼ P
↑ = current(K)
```

Новое состояние контекста создаёт новое точное вхождение и не изменяет предыдущее задним числом.

## А10. Исходная запись отделена от формы

```text
байты
→ каноническое содержимое
→ вхождение исходной записи
→ разбиение
→ D
→ G / T
→ F
```

Написание знака не является самостоятельным семантическим авторитетом.

## А11. Словарь и теория явны

Словарь и теория существуют как сети связей.

Пример состояния словаря:

```text
D = D ⟼ (parentScope ⟼ localHistory)
```

Допуск:

```text
G ⟼ Form
T ⟼ Form
T ⟼ Rule
```

## А12. Определение является явным изменением

Добавление определения создаёт новое состояние словаря и сохраняет прежнее.

Схематично:

```text
Entry      = sourceContent ⟼ form
Occurrence = D_before ⟼ Entry
H_after    = H_before ⟼ Occurrence
D_after    = D_after ⟼ (sameParent ⟼ H_after)
```

Знак `:` не означает автоматически равенство или доказанность.

## А13. Чтение отделено от изменения

```text
читать / искать / разрешать / проверять
                    ≠
создавать / удалять / изменять
```

Отсутствие найденной связи не даёт права создать её скрыто.

## А14. Равенство локально

```text
Pair    = member ⟼ representative
Binding = K ⟼ Pair
```

```text
Equal_K(a,b)
⇔ rep_K(a) is rep_K(b)
```

Рассматривается один явный шаг представителя. Автоматически не следуют глобальная подстановка, транзитивное замыкание, конгруэнтность и равенство по форме сети.

## А15. Правило вывода требует допуска теорией

Любое дополнительное правило предъявляется явно:

```text
T ⟼ Rule
```

Правило не становится частью равенства только потому, что кажется естественным для реализации.

## А16. Конкретный акт отличен от вычислителя

Следует различать:

```text
I — вычислитель
A — конкретный произошедший акт
```

`A` связывает точные свидетельства исходной записи, словаря, теории, контекста, подстановок и результата.

## А17. Последовательность актов хранит происхождение

```text
Run_0 = R
Run_(i+1) = Run_i ⟼ A_i
```

Соседние акты требуют точного совпадения переходного контекста:

```text
A_i.after is A_(i+1).before
```

Такая последовательность не вводит логическую транзитивность сама по себе.

## А18. Циклы и множественность допустимы

Асеть может содержать самозамыкание, взаимные циклы, совместное использование и несколько точных вхождений одной пары полюсов.

Древовидное описание ачислом не является универсальным тождеством произвольной асети.

## А19. Долговременное тождество не равно физическому адресу

Различаются:

```text
оперативная ссылка
долговременный логический идентификатор
номер в снимке
физический адрес
```

Перемещение записи в хранилище не должно менять смысловое тождество связи.

## А20. Остенсивная нотация предшествует машинной

Правильный порядок представления:

```text
∞ / ♂e / b♀ / b⟼e
→ структурные уравнения
→ точные вхождения
→ машинное представление
```

`Link(start,end)` является техническим носителем уже понятой структуры, а не заменой языка МТС.

## Историческая граница

Принятая версия 0.2 использовала обозначения:

```text
♀F / F♂
```

Они относятся к исторической семантике проекций и не определяют текущее самозамыкание.

Текущая ориентация остаётся:

```text
♂e = S = S ⟼ e
b♀ = E = b ⟼ E
```

## Сжатая система

```text
А0   всё есть связь
А1   различённый акорень ∞
А2   четыре остенсивные формы
А3   самозамыкание не является типом
А4   бинарность и направленность
А5   R/O/C/L/U и [ ] 1 0
А6   точное вхождение
А7   роли выражаются связями
А8   форма отделена от действия
А9   явный K и ↑
А10  исходная запись отделена от формы
А11  явные D/G/T
А12  явное изменение через :
А13  чтение отделено от изменения
А14  локальное равенство
А15  явный допуск правил
А16  вычислитель отличен от акта
А17  точная последовательность актов
А18  циклы и множественность допустимы
А19  логическое тождество не равно адресу
А20  остенсивность предшествует машине
```

Эти аксиомы задают текущую границу кандидата. Всё, что выходит за неё, требует отдельного решения, а не молчаливого расширения через код или документацию.
