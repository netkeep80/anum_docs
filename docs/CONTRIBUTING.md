# Руководство по вкладу

Изменения активного дерева вносятся через pull request и проходят blocking CI + repo-guard.

Главный принцип репозитория:

```text
main = current state
Git / closed Issues / closed PRs = history
ONE topic -> ONE active owner
```

Исследовательская история не должна копироваться в новые Markdown-файлы только потому, что она была полезна в процессе. Новый документ добавляется лишь тогда, когда он является отдельной актуальной нормативной или технической поверхностью.

## Канонические области

| Содержимое | Каталог |
|---|---|
| текущее ядро TypeScript | `ts/src/` |
| исполнимые проверки | `ts/test/` |
| браузерная интеграция | `ts/browser/` |
| версионированные машинные контракты | `contracts/` |
| свидетельства принятия | `cutover/` |
| нормативная теория | `docs/theory/` |
| актуальные технические спецификации | `docs/specs/` |
| первичный авторский research-source | `docs/research/Исходные мысли МТС.md` |

`docs/research/Исходные мысли МТС.md` защищён: он изменяется только владельцем репозитория. Автоматизированный PR не редактирует и не удаляет этот файл.

Промежуточные research notes, design plans, handoff-документы и альтернативные архитектурные проекции не сохраняются в active tree; для них используются Issues, PR discussion и Git history.

<!-- мтс-текущая-проекция:начало -->
> **Автоматическая проекция текущей машинной границы.** Этот блок строится из принятого контракта командой `npm --prefix ts run docs:sync`; содержательную теорию вне блока команда не меняет.
>
> - Текущий выпуск: `mts-contract/v0.11` + `mts-conformance/v0.11`; предыдущая неизменяемая пара: `mts-contract/v0.10` + `mts-conformance/v0.10`.
> - Семантическая база: `mts-contract/v0.10`; наблюдаемое смысловое изменение: `да`.
> - Реализация: `TypeScript`; единственная действующая смысловая среда: `да`; Python присутствует: `нет`; режим совместимости выбираем: `нет`.
> - Внутренние знаки F1: `∞ [ ] 1 0 ( ) ⟼ : = .`; отложены: `≡ ♂ ♀ ↑ { } ¬ ↛`; только метаязык: `∧ ⇔ ⇒ ≠ →`.
> - Корневой базис: `R: R = R ⟼ R; O: O = O ⟼ R; C: C = R ⟼ C; L: L = O ⟼ C; U: U = C ⟼ O`.
> - Чтение может материализовать: `нет`; отсутствие найденной записи доказывает несуществование: `нет`.
> - Строковый носитель: единица `byte`, конверт `[bbbbbbbb]`, бит на конверт `8`, порядок `MSB-first`.
<!-- мтс-текущая-проекция:конец -->

## Current lifecycle

```text
accepted/current = MTS v0.11
candidate = MTS v0.12 / NOT ACCEPTED
candidate lifecycle owner = #1134
foundation/architecture owner = #1167
C7+ = BLOCKED until #1167 is resolved
global roadmap = #657
```

Не создавать параллельную foundation/lifecycle issue для мысли, которая ещё относится к #1167. Отдельная issue допустима для самостоятельно исполнимого bounded deliverable после того, как его зависимость и owner определены в roadmap.

## Первичный язык фундамента

При изложении основания порядок представлений обязателен:

```text
1. остенсивная форма МТС: ∞ / ♂e / b♀ / b⟼e
2. структурное уравнение
3. machine/reference representation, например Link(start,end)
```

Вторичная host-нотация не должна вытеснять остенсивные формы. Одинаковый видимый знак на разных уровнях не получает одинаковый смысл автоматически: source glyph, dictionary Entry, semantic Use и root abit необходимо различать.

## Иерархия источников истины

1. Нормативная теория определяет принятый смысл.
2. Принятый машинный контракт фиксирует переносимую наблюдаемую проекцию.
3. Корпус соответствия делает границу проверяемой.
4. Эталонная реализация свидетельствует реализуемость.
5. Git, Issues и PR хранят исследовательскую историю.

Для непринятого кандидата lifecycle/foundation owner может зафиксировать, что candidate contract требует bounded correction. До явного принятия такой candidate не переписывает accepted theory.

## Contract-driven изменение

Для смыслового изменения:

```text
problem / falsifier
→ решение в единственном active owner
→ нормативная формулировка
→ candidate contract + conformance
→ implementation/evidence
→ docs:sync + docs:check
→ CI + repo-guard
→ explicit acceptance or rejection
```

Реализация не является источником семантики:

```text
реализация делает X
!=
МТС означает X
```

Команды проверки:

```bash
npm --prefix ts run docs:sync
npm --prefix ts run docs:check
npm --prefix ts run check
```

`docs:sync` изменяет только блоки между `мтс-текущая-проекция:*`; содержательную теорию из TypeScript он не выводит.

## Anti-sprawl gate

Перед добавлением Issue или Markdown-файла необходимо ответить на три вопроса:

```text
есть ли уже active owner этой темы?
содержит ли новый артефакт уникальную current authority/evidence?
нельзя ли сохранить обсуждение только в существующей Issue/Git history?
```

Если ответы указывают на дублирование, новый артефакт не создаётся.

При реорганизации предпочтителен отрицательный рост поверхности:

```text
active semantic paths after <= before
current documents after <= before
history remains recoverable from Git
```

## Язык

Нормативная теория и определения МТС излагаются по-русски. Английские имена используются для буквальных программных сущностей, протоколов и полей.

Новый теоретический термин сначала определяется в языке самой МТС, а программное имя получает только при необходимости реализации.
